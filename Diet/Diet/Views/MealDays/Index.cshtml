@model IEnumerable<Diet.Data.MealDay>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MealTypes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meals)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MealTypes.MealTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Meals.MealId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MealDayId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MealDayId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MealDayId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
